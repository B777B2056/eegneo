#pragma once

// 颜色枚举
enum BackgroundColor
{Green, Yellow, Red};

// 功率谱类型枚举
enum PSDType
{ EMPTY, Linear, Log };

// 滤波器枚举
enum FilterType
{BandPass, Notch};

// 设备厂商枚举
enum BoardType
{Null, Shanghai, Shanxi};
